# CESI Backend - Python Dependencies
# Install with:  pip install -r requirements.txt

# Django framework
django>=5.1,<7.0

# Django REST Framework (API endpoints)
djangorestframework>=3.15,<4.0

# CORS headers (allows frontend on :5173 to talk to backend on :8000)
django-cors-headers>=4.3,<5.0

# Pillow (image processing for avatar uploads)
Pillow>=12.0,<13.0

# cryptography (Fernet encryption for messaging module)
cryptography>=46.0,<47.0

# Optional package for Quill-like rich text field support in Django backend (frontend uses react-quill via npm)
django-quill-editor>=0.1.42

# In case of direct backend quill usage - no backend advanced Quill packages currently used
# Frontend dependencies are in FrontEnd/Main/package.json and managed by npm/yarn.
# Production Web Server
gunicorn>=20.1,<21.0

dj-database-url
psycopg2-binary
